package freed.uc2rest.enums;

// "array", "full", "single", "off", "left", "right", "top", "bottom",
public enum LedModes {
    array,
    full,
    single,
    off,
    left,
    right,
    top,
    bottom
}
